<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/vendorRecyclerView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_above="@id/vendorMessageInput"
        android:layout_alignParentTop="true"
        android:layout_marginTop="134dp"
        android:layout_marginBottom="36dp" />

    <EditText
        android:id="@+id/vendorMessageInput"
        android:layout_alignParentBottom="true"
        android:layout_toStartOf="@id/vendorSendButton"
        android:layout_toLeftOf="@id/vendorSendButton"
        android:layout_width="289dp"
        android:layout_height="69dp"
        android:layout_weight="1"
        android:hint="Type a message" />


    <Button
        android:id="@+id/vendorSendButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentBottom="true"
        android:text="Send" />

    <Button
        android:id="@+id/top2"
        android:layout_width="380dp"
        android:layout_height="58dp"
        android:layout_alignParentStart="true"
        android:layout_alignParentEnd="true"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:text="Chat to User"
        android:textColorLink="#CF93DA" />



</RelativeLayout>